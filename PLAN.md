# Пошаговый план реализации Bill Bot (Alligator EMA + подтверждённые фракталы) для Hyperliquid

## Реализовано
- Шаг 1: создан каркас проекта (Python 3.13) и инфраструктура запуска (Docker + защищённый Redis).
  - Добавлены файлы инфраструктуры:
    - `requirements.txt` (зависимости: aiohttp, redis)
    - `Dockerfile` (сборка образа, запуск `python -m bill_bot.main`)
    - `docker-compose.yml` (сервисы `bot` и `redis`)
    - `.env.example` (пример переменных окружения, включая `REDIS_PASSWORD`)
  - Защита Redis:
    - Redis стартует с `--requirepass ${REDIS_PASSWORD}`
    - Подключение бота к Redis идёт по URL с паролем (если пароль задан)
  - Каркас приложения:
    - `bill_bot/` пакет
    - `bill_bot/core/config.py` (чтение env)
    - `bill_bot/core/redis_client.py` (инициализация Redis-клиента)
    - `bill_bot/main.py` (bootstrap: Redis PING и дальнейший цикл ожидания; dry-run)
  - Верификация:
    - `python -m compileall bill_bot` проходит без ошибок

- Шаг 2: DataFeed (MVP) — догрузка истории 15m и фиксация закрытых свечей.
  - Реализован клиент Hyperliquid `candleSnapshot` через POST `/info`.
  - При старте по `PAIRS`:
    - догружается история и сохраняются последние `HISTORY_BARS` закрытых свечей в Redis
    - ключи: `candles:<pair>:15m` (JSON-массив), `candles_meta:<pair>_15m` (count, ts_from, ts_to)
  - Реализован polling закрытия свечей:
    - раз в `POLL_SECONDS` запрашивается небольшой диапазон последних свечей
    - при появлении новой закрытой свечи она добавляется в окно и логируется как `New closed candle`

- Шаг 3: Indicators (MVP) — Alligator EMA(13/8/5) и определение “сон/не сон” по SpreadLines.
  - Добавлен расчёт EMA и линий Alligator по Close: Jaw=13, Teeth=8, Lips=5.
  - Добавлен SpreadLines = max(Jaw,Teeth,Lips) - min(Jaw,Teeth,Lips).
  - Добавлен критерий сна:
    - берём медиану SpreadLines по окну `SLEEP_WINDOW` (по умолчанию 20)
    - сравниваем с порогом `SLEEP_K * last_close` (по умолчанию 0.001)
  - Сохраняем последнее состояние индикаторов в Redis:
    - ключ `ind:last:<pair>:15m` (JSON: jaw/teeth/lips/spread/sleep/параметры сна)
  - В логи пишется строка `Alligator: ... sleep=... med_spread=...`

- Шаг 4: FractalDetector (MVP) — подтверждённые кластера (фракталы) по окну 5 свечей.
  - Добавлен детектор подтверждённых фракталов:
    - HIGH: High[i] выше High[i-1], High[i-2], High[i+1], High[i+2]
    - LOW: Low[i] ниже Low[i-1], Low[i-2], Low[i+1], Low[i+2]
    - подтверждение происходит при наличии 2 следующих свечей (в realtime проверяем центр = len(window)-3)
  - Для каждого фрактала сохраняем: kind, t (время центра), price (high/low), close свечи центра, teeth на свече центра.
  - Хранение в Redis:
    - ключ `fractals:<pair>:15m` (JSON-список, ограничение `FRACTALS_MAX`)
  - В логи пишется строка `Fractal confirmed: ...`

- Шаг 5: StrategyEngine (MVP) — генерация “кандидата сигнала” по правилам Bill Williams.
  - Условия кандидата:
    - sleep=true (из SpreadLines)
    - LONG: последний HIGH-фрактал, у которого close кластера > teeth кластера
    - SHORT: последний LOW-фрактал, у которого close кластера < teeth кластера
  - Вход:
    - stop-market trigger = cluster_price ± 1 tick (tick получаем из API Hyperliquid по `szDecimals` и правилам tick size)
  - SL:
    - LONG: ближайший LOW-фрактал до HIGH-кластера, у которого close < teeth (на свече кластера)
    - SHORT: ближайший HIGH-фрактал до LOW-кластера, у которого close > teeth (на свече кластера)
  - TP:
    - RR = 1 : 1.5
  - Хранение кандидата в Redis:
    - `signal_candidate:last:<pair>:15m`
  - В логи пишется строка `Signal candidate: ...`

- Шаг 6: Tick size из API (улучшение шага 5).
  - Бот получает `szDecimals` инструмента через Hyperliquid `/info` type=`meta`.
  - Tick size вычисляется динамически от цены кластера по правилам Hyperliquid:
    - ограничение по числу знаков после запятой: `MAX_DECIMALS - szDecimals` (для perps `MAX_DECIMALS=6`)
    - ограничение по 5 значащим цифрам
    - итоговый tick = max(tick_by_decimals, tick_by_sigfig)
  - `TICK_SIZE` остаётся как fallback, если API недоступен или инструмент не найден.

- Шаг 7: ExecutionDryRun (MVP) — виртуальные ордера/позиции и P&L.
  - После появления `signal_candidate:last:<pair>:15m` бот может поставить dry-run ордер, если нет активного ордера/позиции.
  - Размер позиции:
    - пока берём из env: `VIRTUAL_EQUITY` и `RISK_PCT`
    - qty = (equity * risk_pct/100) / abs(entry - SL)
  - Исполнение:
    - LONG: ордер срабатывает, если high свечи >= trigger
    - SHORT: ордер срабатывает, если low свечи <= trigger
    - entry price в MVP = trigger
  - Сопровождение:
    - SL/TP проверяются по диапазону свечи
    - если SL и TP в одной свече: в MVP считаем, что сработал SL (консервативно)
  - Хранение в Redis:
    - `state:<pair>:15m` (ord/pos/last_trade)
    - `pnl:<pair>:15m` (unrealized/realized)
  - В логи пишутся события: `Dry-run order placed`, `Dry-run event: position_opened/position_closed`

- Шаг 8: Telegram (MVP) — управление подписками (пары/risk/старт/стоп).
  - Добавлен Telegram бот (polling) и простое меню:
    - выбор пар (multi-select) из `PAIRS_AVAILABLE`
    - выбор risk% кнопками (0.5/1/2/3/5)
    - старт/стоп отслеживания (флаг active)
    - статус (текущие выбранные пары и риск)
  - Подписки храним в Redis:
    - пары: `sub:user:<user_id>:pairs:15m`
    - активность: `sub:user:<user_id>:active:15m`
    - риск: `sub:user:<user_id>:cfg:15m`
    - индекс пользователей по паре: `sub:pair:<pair>:15m:users`
    - активные пары (объединение): `active_pairs:15m`
  - Market loop теперь обрабатывает пары из `active_pairs:15m` (если пусто, использует fallback `PAIRS` из env).

- Шаг 9: Персональные позиции и P&L (MVP) — учёт по каждому пользователю.
  - Dry-run состояние и P&L перенесены на ключи с user_id:
    - `state:<user_id>:<pair>:15m`
    - `pnl:<user_id>:<pair>:15m`
  - Risk% берётся из Redis-конфига пользователя (`sub:user:<user_id>:cfg:15m`), по умолчанию `RISK_PCT` из env.
  - В Telegram добавлены экраны:
    - “📈 Позиции”: показывает по выбранным парам текущий ордер/позицию и uPnL
    - “💰 P&L”: суммарный unrealized/realized по выбранным парам

- Шаг 10: График (MVP) — отправка PNG с аллигатором и кластерами в Telegram.
  - Добавлена кнопка “📉 График” в меню Telegram.
  - По нажатию показывается список выбранных пар пользователя.
  - Бот формирует PNG:
    - свечи (candles)
    - линии Alligator (Jaw/Teeth/Lips)
    - отметки фракталов (HIGH/LOW)
  - Источник данных: Redis window `candles:<pair>:15m` и фракталы из Redis.

- Шаг 11: Выбор таймфрейма (TF) на пользователя + обработка нескольких TF.
  - В Telegram добавлена кнопка “⏱ TF” для выбора таймфрейма из `TIMEFRAMES_AVAILABLE`.
  - Выбранный TF пользователя хранится в Redis: `sub:user:<user_id>:timeframe`.
  - Все экраны (статус/пары/риск/позиции/P&L/график) теперь работают в контексте выбранного TF.
  - Market loop запускается для каждого TF из `TIMEFRAMES_AVAILABLE` и использует tf-скоупнутые Redis-ключи:
    - свечи: `candles:<pair>:<tf>`
    - фракталы: `fractals:<pair>:<tf>`
    - подписки: `sub:user:<user_id>:pairs:<tf>`, `sub:user:<user_id>:active:<tf>`, `sub:user:<user_id>:cfg:<tf>`
    - состояния: `state:<user_id>:<pair>:<tf>`, `pnl:<user_id>:<pair>:<tf>`

- Шаг 12: Смена TF без “призрачного” трекинга (качество UX).
  - При смене TF:
    - переносим пары в новый TF, если в новом TF ещё нет выбранных пар
    - переносим risk%, если в новом TF он ещё не задан
    - выключаем active на старом и новом TF (нужно заново нажать “▶️ Запуск”)

- Шаг 13: Главное меню Telegram через Reply Keyboard (MVP UX).
  - Основная навигация вынесена в Reply Keyboard (кнопки снизу).
  - Inline-клавиатуры остаются для экранов выбора (пары/TF/риск) и для выбора пары в “📉 График”.

- Шаг 14: График с уровнями сделки (визуализация ордера/позиции).
  - На PNG-график добавлены уровни:
    - если есть pending order: Trigger / SL / TP
    - если есть позиция: Entry / SL / TP
  - Оптимизирована отрисовка фракталов (индекс по timestamp, без O(N^2)).

- Шаг 15: Фикс расчёта Alligator на графике.
  - Исправлено использование `alligator_ema`: берём jaw/teeth/lips из возвращаемого dict.

- Шаг 16: Журнал сделок (history) + экран в Telegram.
  - При закрытии позиции dry-run пишет сделку в Redis list: `trades:<user_id>:<pair>:<tf>` (LPUSH + LTRIM).
  - В Telegram добавлена кнопка “📜 Сделки” (показывает последние сделки по выбранным парам/TF).

- Шаг 17: README.md (полная инструкция).
  - Добавлен README.md: установка (Docker/локально), настройка `.env`, запуск, просмотр логов, использование Telegram UI, ключи Redis.

- Шаг 18: Фикс Docker-сборки (совместимость зависимостей).
  - Исправлен конфликт зависимостей `aiogram`/`aiohttp` в requirements.txt для успешной сборки Docker-образа.

- Шаг 19: README — Docker команды + troubleshooting.
  - Добавлен раздел с командами Docker Compose и типовыми сценариями диагностики.

- Шаг 20: UX и логирование Telegram + ручной ввод пары + фикс графика.
  - Добавлено подробное логирование действий в Telegram (кнопки/колбэки/изменения настроек).
  - В “📌 Пары” добавлен ручной ввод пары (пример: HYPE-USDC).
  - Кнопка запуска стала переключателем (Запуск ↔ Стоп).
  - Улучшена отрисовка кластеров на графике (выше контраст/поиск по ближайшей свече при необходимости).

- Шаг 21: Фикс кластеров на графике + ввод произвольной пары в “📉 График”.
  - Кластера (фракталы) на графике теперь считаются из текущего окна свечей, а не берутся из Redis-хранилища фракталов.
  - В меню графика добавлена кнопка “⌨️ Ввести пару” (можно вводить HYPE или HYPE-USDC).
  - Если по введённой паре нет свечей в Redis, бот догружает их из Hyperliquid и строит график (Alligator + кластера).

- Шаг 22: Git workflow + Docker rebuild.
  - Фиксации ведём в ветке `bill_bot` и пушим в `origin/bill_bot` (не в main).
  - Для последнего шага обновлён commit message (подробно на русском) и изменения отправлены в `origin/bill_bot`.
  - Docker пересобран без кеша и перезапущен.

- Шаг 23: Фикс NameError в Telegram (“📌 Пары”).
  - Восстановлена функция `_pairs_menu` в `telegram_bot.py` (раньше тело меню пар оказалось внутри другой функции, из-за чего в рантайме падало с `NameError: _pairs_menu not defined`).

- Шаг 24: Уточнение отрисовки кластеров на графике (строгое правило 2+2).
  - Для построения графика кластера (фракталы) рассчитываются напрямую по OHLC: HIGH если high центральной свечи строго больше high двух слева и двух справа; LOW если low центральной свечи строго меньше low двух слева и двух справа.
  - В `build_chart_png` добавлена поддержка точного позиционирования кластера по индексу свечи (idx), чтобы исключить ошибки привязки по timestamp.

- Шаг 25: Формат даты на графике + улучшение вывода сделок.
  - Подписи оси X на графике переведены в формат `HH:MM DD/MM/YY`.
  - Экран “📜 Сделки” сделан более читаемым: эмоджи, формат даты/времени как на графике, добавлен вывод баланса (виртуальный баланс = база + realized + unrealized).

- Шаг 26: Telegram-сигналы по событиям стратегии/исполнения.
  - Добавлены уведомления в Telegram при: найденном сетапе (SignalCandidate), открытии позиции и закрытии позиции.
  - Сообщения включают ключевые параметры (entry/SL/TP/qty), формат времени `HH:MM DD/MM/YY`, а при закрытии — P&L и баланс (база + realized + unrealized).

- Шаг 27: Сигналы с графиком (сетап/вход/выход).
  - Каждое уведомление (сетап/открытие/закрытие) теперь отправляет картинку графика с линиями уровней (Cluster/Trigger/Entry/Exit/SL/TP) и фракталами.

- Шаг 28: Улучшение экрана “Позиции / ордера” + управление (отмена/закрытие).
  - Экран “📈 Позиции” сделан более читаемым (эмоджи, сводка баланса, форматированные строки).
  - Добавлены кнопки: отмена ордера и ручное закрытие позиции для каждой пары, плюс кнопка “Обновить”.
  - Экран “📜 Сделки” дополнен: qty, период сделки (opened→closed), причина “Закрыто вручную”.

- Шаг 29: Удаление сообщений (ручное и авто).
  - Добавлена кнопка “🗑 Удалить” на сообщениях бота (включая сигналы с графиком, графики, “📈 Позиции”, “📜 Сделки”).
  - Автоудаление input-сообщений пользователя при нажатии Reply-кнопок (Сделки/Позиции/P&L/Пары/TF/Риск/Статус/График/Запуск/Стоп) и при вводе пары в ручных сценариях.

- Шаг 30: Корректный экран “Меню” после кнопки “⬅️ Назад”.
  - Исправлено определение user_id для “Назад” (раньше брался id бота из cb.message, из-за чего меню показывало неправильные пары/статус).
  - Текст “Меню” сделан читабельнее: эмоджи + добавлен блок P&L (баланс/realized/unrealized).

- Шаг 31: Обновление логики сетапов и ордеров (замена + два стоп-ордера).
  - Если появился новый валидный кластер (LONG: ниже предыдущего HIGH, но выше Teeth; SHORT: выше предыдущего LOW, но ниже Teeth) и позиции нет — предыдущий ордер по стороне заменяется на новый.
  - Если Alligator “спит”, допускается постановка двух стоп-ордеров одновременно (LONG и SHORT) до момента открытия позиции.

- Шаг 32: Улучшение P&L/Статуса + удаление сообщений.
  - Экран “💰 P&L” переработан: баланс + эмоджи + знаки +/- для realized/unrealized; добавлена кнопка “🗑 Удалить”.
  - Для сообщений “Статус” и “P&L” добавлена возможность удаления (inline “🗑 Удалить” и Reply-кнопка “🗑 Удалить” удаляет последнее сообщение бота).

- Шаг 33: Часовой пояс Варшавы (графики и сообщения).
  - Время на графиках и в текстовых датах переведено в Europe/Warsaw (вместо UTC), формат `HH:MM DD/MM/YY`.

- Шаг 34: Синхронизация времени сетапа с графиком + RR=1:1.
  - Время сетапа/открытия/закрытия в сообщениях выровнено по времени закрытия свечи (Candle.T), чтобы совпадало с осью времени на графике.
  - Risk:Reward изменён на 1:1 (TP = 1 * SL).

- Шаг 35: Линии Entry/SL/TP на графиках под RR=1:1.
  - Для графика “📉 График” TP на линиях рассчитывается по текущему RR=1:1 от Entry и SL (для ордера — от trigger, для позиции — от entry).

- Шаг 36: Экспорт сделок в Excel + кнопка скачивания.
  - Сделки сохраняются в Redis history до TRADES_MAX (по умолчанию 5000).
  - В “📜 Сделки” добавлена кнопка “📥 Excel” для выгрузки отчёта .xlsx (лист Trades + Summary).

- Шаг 37: Trailing SL/TP по прогрессу к тейку.
  - LONG: при достижении 60% пути к текущему TP — SL в безубыток (entry).
  - LONG: при достижении 95% пути к текущему TP — SL переносится на 85% пути, а TP расширяется до 150% от текущей дистанции; цикл повторяется дальше.
  - SHORT: зеркально (используется минимум свечи).

- Шаг 38: README — актуализация под текущую версию.
  - Обновлено описание стратегии: фракталы 2+2, RR=1:1, правила trailing (включая циклическое продолжение).
  - Добавлены/уточнены возможности: два стоп-ордера при sleep, замена ордера на новый сетап, сигналы с графиком, Excel-выгрузка, Europe/Warsaw, удаление сообщений.

- Шаг 39: Настройка RR из Telegram.
  - Добавлена кнопка “📐 RR” и выбор RR (пресеты + ручной ввод).
  - RR хранится в пользовательском TF-конфиге (Redis), используется при расчёте TP и при замене ордера, если RR поменялся.

- Шаг 40: Excel — статистика по каждой паре.
  - В отчёт добавлены агрегаты по каждой паре: количество TP(+MANUAL_CLOSE), SL, winrate% и суммарный P&L.

- Шаг 41: Фикс логики сетапов (последние кластера + sleep по пересечению линий).
  - Для сетапа берём последние валидные кластера относительно Teeth: entry-кластер по стороне и SL-кластер противоположной стороны.
  - Замена entry-кластера: LONG — только если новый кластер ниже предыдущего и выше Teeth; SHORT — только если новый кластер выше предыдущего и ниже Teeth.
  - “Сон” аллигатора требует пересечения линий (jaw/teeth/lips) + низкий spread на окне.

- Шаг 42: Сопровождение ордеров — обновление SL и удержание противоположного ордера.
  - Если entry-кластер не меняется, но появляется новый противоположный кластер, который “подтягивает” стоп (LONG: выше предыдущего SL, SHORT: ниже предыдущего SL) — обновляем SL/TP и сетап-график.
  - При открытии позиции противоположный ордер сохраняется до момента безубытка; при переходе SL в безубыток — противоположный ордер отменяется.
  - Если позиция закрылась до безубытка и противоположный ордер уже был триггернут на этой свече — позиция открывается сразу (в ту же свечу).

## 0) Зафиксированные требования
- Рынок: Hyperliquid.
- TF: 15m.
- Alligator:
  - Jaw = EMA(Close, 13) (синий)
  - Teeth = EMA(Close, 8) (красный)
  - Lips = EMA(Close, 5) (зелёный)
- Кластера (фракталы, только подтверждённые):
  - LONG-кластер: High[i] > High[i-1], High[i-2], High[i+1], High[i+2]
  - SHORT-кластер: Low[i] < Low[i-1], Low[i-2], Low[i+1], Low[i+2]
  - Используем только подтверждённые фракталы (задержка 2 свечи).
- Сетап LONG:
  - Аллигатор “спит” на последних N свечах (для старта N=20).
  - Появился подтверждённый LONG-кластер, и свеча кластера закрылась выше Teeth (красной).
  - Вход: stop-market на пробой уровня кластера + 1 tick.
  - SL: ближайший противоположный кластер (SHORT-фрактал), при этом свеча кластера закрылась ниже Teeth.
- TP: RR = 1 : 1 (TP = entry + 1*(entry - SL)).
- Сетап SHORT: зеркально.
- Критерий “сон”: пересечение линий Alligator (jaw/teeth/lips) + низкий spread (SpreadLines) на окне N.
- Риск: задаётся в Telegram в % от депозита.
- Режим исполнения на старте: dry-run (учёт ордеров/позиций/PNL без реальной торговли).
- Стек: Python 3.13 + Redis (защищённый) + Docker.
- Telegram UX:
  - выбор пары (multi-select) и TF
  - запуск отслеживания выбранных пар (догрузка 100 бар + обработка каждой закрытой 15m свечи)
  - просмотр отслеживаемых пар (список + открытые позиции + P&L)
  - отслеживание P&L (реализованный и нереализованный)
  - просмотр графика выбранной пары с Alligator + кластерами
- ML/LM: анализ предыдущих сделок и коррекция последующих (адаптация параметров), без изменения базовой логики вход/SL/TP “на лету”.

## 1) Целевой результат (MVP)
MVP считается готовым, когда бот:
- стабильно получает 15m свечи по выбранным парам;
- детектит подтверждённые фракталы, рассчитывает Alligator EMA 13/8/5;
- определяет “сон”, генерирует сигналы, выставляет dry-run entry/SL/TP;
- ведёт позиции и P&L (реализованный/нереализованный) в разрезе пар;
- отдаёт в Telegram: управление подписками, статус, P&L, график с разметкой.

## 2) Архитектура (модули)
Минимальный состав модулей:
- DataFeed
  - подписка на данные Hyperliquid
  - сбор/обновление 15m свечей
  - начальная догрузка истории (100 бар)
- Indicators
  - EMA(13/8/5) по Close
  - метрика SpreadLines
- FractalDetector
  - выявление/подтверждение фракталов (5 свечей)
  - хранение списка последних подтверждённых кластеров
- StrategyEngine
  - sleep-condition
  - правила входа/SL/TP
  - генерация торгового намерения (intent)
- ExecutionDryRun
  - симуляция stop-market ордера (trigger=cluster±tick)
  - логика “ордер сработал на свече”
  - сопровождение позиции, закрытие по SL/TP
- Portfolio / PnL
  - учёт позиции (side, entry, size, SL, TP)
  - unrealized P&L по mark price
  - realized P&L при закрытии
- Storage (Redis)
  - свечи, кластера, состояние ордеров/позиций, журнал сделок
- Telegram UI
  - выбор пар, TF
  - запуск/остановка отслеживания
  - статус, позиции, P&L
  - запрос графика по паре
- ML Tuner (позже, но закладываем интерфейсы)
  - сбор истории сделок
  - расчёт метрик и подбор параметров
  - применение параметров через конфиг-слой

## 3) Redis (защищённый) + Docker
Шаги:
1) Docker-compose: сервисы bot + redis.
2) Защита Redis:
   - пароль (requirepass через env)
   - не публиковать порт наружу без необходимости (либо ограничить bind/Firewall)
   - отдельная docker network
3) Хранилища/ключи (черновая схема):
   - `candles:<pair>:15m` (последние 100–300 свечей)
   - `fractals:<pair>:15m` (последние подтверждённые фракталы)
   - `state:<pair>:15m` (активный ордер/позиция)
   - `pnl:<pair>` (агрегаты P&L)
   - `trades:<pair>` (журнал закрытых сделок)
   - `subs:<user_id>` (выбранные пары, TF, risk%)

Проверка готовности шага:
- Redis стартует с паролем, бот подключается по password.
- У Redis нет “лишней” публикации портов наружу (если не требуется).

## 4) DataFeed: свечи 15m (история 100 бар + realtime)
Шаги:
1) Выбор источника:
   - получать 15m свечи напрямую (если API позволяет), иначе строить из более мелких.
2) При старте подписки:
   - загрузить последние 100 15m баров.
3) Realtime:
   - по закрытию каждой 15m свечи обновлять хранилище и запускать расчёты.

Проверка готовности шага:
- Для каждой выбранной пары есть стабильный поток закрытых 15m свечей.
- В Redis всегда поддерживается окно минимум 100 свечей.

## 5) Indicators: Alligator EMA + SpreadLines (сон)
Шаги:
1) Реализовать расчёт EMA(13/8/5) по Close.
2) Реализовать SpreadLines:
   - `spread = max(jaw, teeth, lips) - min(jaw, teeth, lips)`
3) Sleep-condition:
   - на последних N свечах (N=20) спред “маленький”
   - первый вариант порога:
     - `median(spread последние N) < k * price`, где price = last_close
     - k задаём конфигом и подбираем в ходе первых тестов

Проверка готовности шага:
- Значения EMA и spread воспроизводимы на одном и том же наборе свечей.
- В логах видно sleep=true/false с метрикой.

## 6) FractalDetector: подтверждённые кластера
Шаги:
1) Реализовать детектор фракталов по окну 5 свечей.
2) Учесть подтверждение:
   - фрактал на i фиксируем только когда доступны i+1 и i+2
3) Хранить:
   - список последних подтверждённых HIGH-фракталов и LOW-фракталов
   - индекс/время свечи центра фрактала

Проверка готовности шага:
- В логах видно момент подтверждения фрактала.
- Фрактал появляется с задержкой ровно 2 свечи.

## 7) StrategyEngine: правила входа/SL/TP
Шаги:
1) LONG:
   - sleep=true на последних N
   - есть подтверждённый HIGH-фрактал, и close свечи фрактала > Teeth (на свече фрактала)
   - entry trigger = fractal_high + 1 tick
2) SL:
   - выбрать ближайший подтверждённый противоположный LOW-фрактал
   - фильтр: close свечи этого кластера < Teeth (на свече этого кластера)
3) TP:
   - RR 1:1.5
4) SHORT: зеркально.
5) Дедуп:
   - если уже есть активный ордер/позиция по паре — не создавать новый.

Проверка готовности шага:
- На синтетическом наборе свечей стратегия выдаёт ожидаемый intent (entry/SL/TP).

## 8) ExecutionDryRun: симуляция stop-market и сопровождение
Шаги:
1) Постановка entry-ордера (dry-run state):
   - тип: stop-market
   - trigger=fractal±tick
2) Исполнение:
   - на каждой новой закрытой свече проверять, был ли пробой trigger:
     - для LONG: high >= trigger
     - для SHORT: low <= trigger
   - при “срабатывании” фиксировать entry price (вариант MVP: trigger).
3) Сопровождение:
   - проверять SL/TP на каждой новой свече
   - учитывать конфликт “в одной свече и SL и TP” (правило приоритета фиксируем заранее)
4) Запись сделки в журнал + обновление P&L.

Проверка готовности шага:
- Для тестового кейса позиция открывается/закрывается, P&L обновляется.

## 9) P&L: реализованный и нереализованный
Шаги:
1) Unrealized:
   - расчёт от mark/last price.
2) Realized:
   - фиксировать при закрытии сделки.
3) Агрегация:
   - по паре и по пользователю (если мультипользовательский режим).

Проверка готовности шага:
- Команды Telegram показывают корректные цифры для открытой позиции и после закрытия.

## 10) Telegram UX (кнопки и сценарии)
Шаги:
1) Меню:
   - выбор пары (multi-select)
   - выбор TF (в MVP фиксируем 15m, но UI оставляем)
   - ввод risk% (валидация)
2) “Запуск отслеживания”:
   - догрузка 100 баров
   - старт фоновой задачи по паре
3) “Отслеживаемые пары”:
   - список + состояние (нет ордера/есть ордер/в позиции)
   - P&L по каждой паре
4) “P&L”:
   - общий realized/unrealized
5) “График”:
   - выбрать пару → получить картинку

Проверка готовности шага:
- Все кнопки работают без падений, состояние сохраняется между рестартами (через Redis).

## 11) График: Alligator + кластера (15m, окно 100 баров)
Шаги:
1) Генерация графика по 100 последним свечам:
   - свечи OHLC
   - линии EMA 13/8/5
   - маркеры фракталов (high/low)
2) Отправка в Telegram как изображение.

Проверка готовности шага:
- На графике корректно видны линии и отметки кластеров, масштабы читаемые.

## 12) ML/LM: анализ истории сделок и коррекция параметров (этап 2)
Цель: повышать качество сигналов не “магией”, а через адаптацию параметров.

Шаги:
1) TradeJournal:
   - сохранять фичи сетапа на входе + исход сделки.
2) Метрики:
   - winrate, expectancy, PF, maxDD, средний R, распределения по времени суток/волатильности.
3) Коррекция параметров:
   - k для sleep-condition
   - TTL (если добавим) и/или дополнительные фильтры
   - tick buffer (если понадобится)
4) Использование LM:
   - периодически формировать отчёт и просить LM объяснить паттерны + предложить изменения
   - изменения включаются только после подтверждения (в Telegram).

Проверка готовности шага:
- Есть отчёт по сделкам и механизм применения параметров без перезапуска (или с контролируемым перезапуском).

## 13) Контрольные решения, которые нужно закрепить перед кодом
- Правило приоритета, если на одной свече “достигли” и SL, и TP.
- Размер позиции: считаем от депозита “виртуально” или берём баланс из Hyperliquid (даже в dry-run)?
- Мультипользовательский режим нужен сразу или один пользователь (админ)?

## Реализация реальной торговли (LIVE) на Hyperliquid
- Шаг 43: Добавление зависимостей и ключей для Hyperliquid.
  - Добавлена библиотека `hyperliquid-python` и `eth-account` в `requirements.txt`.
  - В `config.py` и `.env.example` добавлены `HYPERLIQUID_WALLET_ADDRESS` и `HYPERLIQUID_PRIVATE_KEY`.
  - Работа ведется в ветке `live_bill_bot`.
- Шаг 44: Telegram UI для выбора режима и маржи.
  - Добавлена кнопка "🕹 Режим" в главное меню для переключения между DRY и LIVE режимами.
  - Обновлено меню "⚙️ Риск": теперь в нем можно выбирать не только риск на сделку (Risk %), но и используемую часть депозита (Margin %).
  - Состояние `trade_mode` и `margin_pct` сохраняется в Redis (профиль пользователя).
- Шаг 45: REST API клиент для реальной торговли (HyperliquidExchangeClient).
  - Написана обертка `HyperliquidExchangeClient` поверх официальной библиотеки `hyperliquid-python`.
  - Добавлены асинхронные методы: `place_order` (лимитные, рыночные, TP/SL), `cancel_order`, `update_leverage`, `market_close`.
  - В `HyperliquidInfoClient` добавлен метод `user_state` для получения реального баланса и позиций аккаунта (clearinghouseState).
- Шаг 46: WebSocket клиент для отслеживания состояния.
  - Создан `HyperliquidWebsocketClient` (через `aiohttp` ws), подписывающийся на `userEvents` (ордера, исполнения) и `webData2` (состояние аккаунта).
  - Клиент интегрирован в `main.py` в виде фоновой задачи `asyncio.create_task`.
- Шаг 47: Движок `ExecutionLiveRun` и маршрутизация.
  - Создан класс `ExecutionLiveRun`, дублирующий интерфейс DRY-режима, но работающий через реальный Hyperliquid API.
  - При `maybe_place_order`: размещается триггерный ордер (Stop Entry) для входа с расчетом объема от реального баланса (`accountValue`).
  - При `on_candle`: синхронизируется стейт с биржей, при появлении новой позиции размещаются TP/SL (reduce-only), при трейлинге старые ордера отменяются и ставятся новые.
  - В `main.py` добавлена инициализация `ExecutionLiveRun` и маршрутизация сигналов в зависимости от значения `trade_mode` (DRY/LIVE).
- Шаг 48: Полировка и проверка кода.
  - Произведена проверка синтаксиса и импортов.
  - Добавлена отмена старых триггерных ордеров перед размещением нового Stop Entry ордера в `ExecutionLiveRun.maybe_place_order`.
- Шаг 49: Безопасный ввод приватного ключа при запуске в Docker.
  - Создан скрипт `start_bot.py`, который безопасно (без вывода символов на экран) запрашивает приватный ключ `HYPERLIQUID_PRIVATE_KEY` у пользователя.
  - `docker-compose.yml` настроен на получение ключа из окружения хоста (`HYPERLIQUID_PRIVATE_KEY=${HYPERLIQUID_PRIVATE_KEY}`).
  - Это позволяет исключить хранение ключа в файлах (включая `.env`) и повышает безопасность запуска на сервере.
- Шаг 50: Реальный баланс в LIVE режиме.
  - В Telegram UI (Меню, Позиции, P&L, Сделки) и в main.py баланс теперь определяется динамически: в LIVE режиме берётся реальный `accountValue` из Hyperliquid API (`clearinghouseState`), в DRY режиме — виртуальный (`VIRTUAL_EQUITY`).
  - Добавлена вспомогательная функция `_get_base_equity()` в `telegram_bot.py` и обновлена `user_balance()` в `main.py`.
  - Во всех экранах строка «🏦 База» теперь отражает реальную или виртуальную базу в зависимости от режима.
- Шаг 51: Объединение балансов (Perps + Spot).
  - Исправлен баг "нулевого баланса": теперь бот суммирует `accountValue` из Perps и доступный USDC/USDT из Spot кошелька.
  - Логика агрегации баланса синхронизирована в `telegram_bot.py`, `main.py` и `execution_live.py`.
- Шаг 52: Исправление ошибок форматирования (Unknown format code 'f').
  - В `HyperliquidExchangeClient` добавлено принудительное приведение типов к `float` и `bool` перед вызовом SDK.
  - В `execution_live.py` параметр `triggerPx` изменен со строки на число.
  - Это устранило краши при выставлении ордеров, вызванные попыткой SDK отформатировать строку как число.
- Шаг 53: Исправление отмены ордеров и SL/TP.
  - Метод `open_orders` переведен на тип `frontendOpenOrders`, что позволило боту видеть триггерные ордера (Stop Entry).
  - Исправлена логика отмены: теперь при замене сигнала бот корректно находит и отменяет старые ордера на бирже.
  - Во все триггерные ордера (SL/TP) добавлен обязательный параметр `tpsl` (`tp` или `sl`), без которого биржа отклоняла запросы.
  - Улучшена синхронизация позиций: бот теперь корректно сопоставляет монеты (напр. `LTC` и `LTC-USDC`) и отправляет уведомления в Telegram об открытии/закрытии реальных позиций.
- Шаг 54: Обработка ошибок Telegram (message is not modified).
  - Добавлена обработка исключения `TelegramBadRequest` при обновлении сообщений.
  - Теперь бот не падает, если пользователь нажимает «Обновить» или переключает настройки, которые уже установлены.
- Шаг 55: Исправление ручного управления и синхронизации.
  - В `telegram_bot.py` реализованы прямые вызовы API (`cancel_all_orders`, `market_close`) при нажатии кнопок в боте.
  - Добавлена отправка уведомлений с графиками и PnL после ручной отмены ордера или закрытия позиции.
  - В `execution_live.py` добавлено принудительное округление цен до 6 знаков для SL/TP (защита от ошибок точности биржи).
  - Улучшена автоматическая отмена старых ордеров: теперь бот всегда очищает старые стоп-входы перед постановкой новых, используя `frontendOpenOrders`.
  - Все действия (автоматические и ручные) теперь подробно логируются с указанием цен, объемов и ID ордеров.
- Шаг 56: Исправление ошибки 'HyperliquidExchangeClient' object has no attribute 'open_orders'.
  - Исправлен некорректный вызов метода получения ордеров в `ExecutionLiveRun`.
  - В `HyperliquidExchangeClient` добавлен недостающий метод `market_close` для ручного закрытия позиций.

- Шаг 60: Мгновенное выставление TP/SL через WebSocket.
  - Реализован обработчик событий WebSocket (`userEvents`). При получении уведомления об исполнении ордера (`fills`), бот немедленно запускает процедуру синхронизации.
  - Добавлен метод `sync_state` в `ExecutionLiveRun`, который позволяет проверить состояние аккаунта и выставить защиту (TP/SL) в любой момент, не дожидаясь закрытия 1-минутной свечи.
  - Логика уведомлений в `main.py` вынесена в общую функцию `handle_engine_events`, которая обслуживает и регулярные обновления по свечам, и мгновенные обновления по WebSocket.
- Шаг 61: Исправление ошибки запуска (TypeError virtual_equity).
  - Исправлен конструктор `ExecutionDryRun` в `main.py`, возвращен обязательный параметр `virtual_equity`.
  - Бот снова запускается корректно.
- Шаг 62: Реализация механизма Master Sync (Биржа — источник правды).
  - Логика `sync_state` полностью переписана: теперь бот принудительно синхронизирует локальное состояние с биржей, если обнаруживает расхождение в направлении или объеме позиции.
  - Добавлен механизм восстановления SL/TP: если в боте нет данных о стопах, он пытается найти их среди открытых триггерных ордеров на бирже.
  - Если на бирже позиция есть, а в боте нет — бот создаст её. Если на бирже закрыта, а в боте висит — бот удалит её.
  - Исправлена проблема, при которой бот игнорировал открытую позицию, если локально висел ордер другого направления.

- Шаг 63: Доработка уведомлений и состояния ордеров при синхронизации.
  - В `ExecutionLiveRun.sync_state` добавлена логика отправки уведомлений о состоянии ордеров при синхронизации:
    - Уведомление о закрытии позиции (`position_closed`).
    - Уведомление об изменении SL/TP (`position_updated`) при трейлинге.
    - Уведомление о восстановлении ордеров (`orders_restored`) при синхронизации.
  - В `main.py` доработан обработчик `on_candle`: убран излишний `await tg_send` для ордеров, которые уже были отправлены ранее (защита от дублей).
  - Обновлены функции построения графиков (`build_setup_chart`, `build_position_chart`) для корректного отображения уровней при обновлении состояния.

     Основные изменения:
Событийный подход: Движки ExecutionLiveRun и ExecutionDryRun теперь генерируют стандартные события (order_placed, position_opened, position_updated и т.д.). Это позволило унифицировать уведомления и гарантировать отправку графика при любом изменении состояния.
Биржа как источник правды: Улучшен механизм sync_state. Теперь WebSocket передает цену исполнения (fill price), что обеспечивает точный расчет PnL и корректное отображение уровней при закрытии сделки (в том числе ручном).
Мгновенный трейлинг: Бот теперь не только подтягивает SL/TP на бирже при движении цены, но и сразу присылает уведомление с обновленным графиком.
Чистка UI: Исправлены ошибки в telegram_bot.py и main.py, устранены дублирующиеся уведомления.




- Шаг 64: Поддержка встречных ордеров и сохранение RR от реального входа.
  - В ExecutionLiveRun.maybe_place_order реализована выборочная отмена: теперь при выставлении LONG-ордера отменяются только старые LONG-ордера, SHORT-ордера остаются нетронутыми.
  - Локальное состояние теперь поддерживает список ords, позволяя одновременно хранить встречные стоп-ордера.
  - В sync_state исправлен выбор ордера для синхронизации уровней: бот теперь ищет PendingOrder именно той стороны, по которой открылась позиция.
  - Добавлено сохранение коэффициента rr в PendingOrder и Position.
  - Реализован автоматический пересчет Take-Profit при обнаружении новой позиции: бот берет real_entry с биржи и рассчитывает TP так, чтобы соблюдался RR (например, 1.5) относительно реальной точки входа, оставляя SL на уровне кластера.
  - Обновлена логика трейлинга: теперь он использует сохраненный в позиции rr вместо захардкоженного значения.

- Шаг 65: Исправление выставления Тейк-Профита и динамический трейлинг.
  - Внедрена селективная отмена ордеров: теперь при обновлении трейлинга или синхронизации позиции бот отменяет только TP/SL (isPositionTpsl), не затрагивая встречные ордера на вход.
  - Исправлена проблема "отсутствующего TP": добавлены индивидуальные блоки обработки ошибок и расширенное логирование для каждого ордера SL/TP.
  - Полностью переработана логика _apply_trailing: теперь тейк-профит динамически отодвигается (шагами по 50% от начальной дистанции) при достижении ценой 85% от текущей цели.
  - Все расчеты трейлинга теперь базируются на tp0, что исключает экспоненциальный рост уровней и делает поведение бота предсказуемым.

- Шаг 66: Упрощение управления ордерами при открытой позиции.
  - Реализована логика полной очистки ордеров по паре при переходе в режим позиции или трейлинга.
  - В sync_state и on_candle теперь отменяются ВООБЩЕ ВСЕ открытые ордера по паре перед выставлением новых TP/SL. Это гарантирует отсутствие "лишних" ордеров и конфликтов.
  - В maybe_place_order сохранена селективная отмена для поддержки одновременных встречных стоп-входов (LONG и SHORT) до момента открытия сделки.

- Шаг 67: Глобальное исправление точности цен (Significant Figures).
  - Реализован универсальный механизм округления цен до 5 значимых цифр (правило Hyperliquid).
  - Исправлена ошибка 'Invalid TP/SL price', возникавшая на дорогих активах (AAVE и др.) из-за избыточной точности расчетных цен TP/SL.
  - Округление внедрено во все цепочки выставления ордеров: входные стопы, синхронизация позиции и трейлинг.
- Шаг 68: Интерактивное управление через Telegram (кнопки управления).
  - В сообщения об ордерах и позициях добавлены интерактивные кнопки:
    - «❌ Отменить ордер»: мгновенная отмена стоп-входа на бирже.
    - «🔻 Закрыть»: рыночное закрытие активной позиции и отмена защитных ордеров.
    - «🛑 SL» / «✅ TP»: возможность ручного изменения уровней стоп-лосса и тейк-профита через ввод цены в чате (FSM-сценарии).
  - Реализовано обновление исходных сообщений после выполнения действий (например, пометка «🆘 ОТМЕНЕН»).

- Шаг 69: Исправление точности P&L и PnL-график.
  - Улучшен расчет P&L: теперь нереализованный P&L считается непрерывно в `sync_state` на основе живых котировок, а реализованный P&L корректно агрегируется по закрытым сделкам.
  - Исправлена ошибка двойного учета баланса в UI: отображается чистый `accountValue` с биржи и детализация P&L бота.
  - Реализована генерация графика доходности (PnL Chart) через `matplotlib`:
    - Темная тема, отображение кривой эквити от первой сделки до текущего момента.
    - Кнопка «🔄 Обновить» для регенерации актуального графика.
  - Секция «📜 Сделки» расширена до 10 последних записей с детальной информацией о каждой сделке.

- Шаг 70: Реальный баланс (Available to Trade), комиссии в PnL, график реального баланса.
  - **Баланс**: во всех экранах Telegram (Меню, Позиции, P&L, Сделки) и в `main.py` баланс теперь берётся из поля `withdrawable` API Hyperliquid вместо `accountValue`. Это соответствует значению «Available to Trade» на бирже — реальная сумма, доступная для открытия новых позиций.
  - **Комиссии в PnL**: добавлен расчёт торговых комиссий (taker fee 0.0456%) при закрытии позиции. PnL теперь показывается как Net PnL = Gross PnL − Fee (open + close). Комиссия рассчитывается как `notional × fee_rate` для обеих сторон сделки.
    - Добавлен параметр `TAKER_FEE_RATE` в config.py (по умолчанию 0.000456 = 0.0456%).
    - В `ExecutionLiveRun` добавлен метод `_calc_fee()` и параметр `fee_rate`.
    - В `telegram_bot.py` ручное закрытие позиции также учитывает комиссию.
  - **График PnL**: переработан `build_pnl_chart()` — теперь использует реальный баланс с биржи (`balance_after`), сохраняемый в каждой записи trade при закрытии позиции. Для старых сделок без `balance_after` используется fallback `base_equity + cumulative_pnl`. Заголовок изменён на «💳 Balance (Current: X USDC)».
  - **Уведомления при закрытии**: сообщения о закрытии позиции обновлены — показывают Net PnL, Fee (если > 0.0001), доступный баланс и Realized PnL. Добавлены эмодзи для лучшей читаемости.
  - **UI/UX**: обновлено форматирование всех экранов:
    - «💳 Доступно» вместо «💼 Баланс» — отражает реальный доступный баланс.
    - «🟩/🟥 Realized/Unrealized» со знаками +/−.
    - Убрана строка «🏦 База» (больше не нужна).
    - «💸 Fee» в уведомлениях о закрытии.
    - «🔄 Закрыто на бирже» для позиций, закрытых через sync.
  - **API**: добавлен метод `all_mids()` в `HyperliquidInfoClient`.
  - **Конфигурация**: добавлены переменные `TAKER_FEE_RATE` и `TELEGRAM_USER_ID` в `.env.example`.

### Шаг 71: Интеграция реальных комиссий и данных сделок из Hyperliquid API
- **API (`hyperliquid_api.py`)**:
  - Добавлен метод `user_fills()` для получения истории недавних сделок пользователя (`userFills`).
  - Добавлен метод `get_real_trade_details()`, который фильтрует полученные сделки по названию монеты и времени открытия (с запасом 1 минута), вычисляет точные средние цены входа/выхода, суммирует реальные комиссии биржи за открытие и закрытие позиции, рассчитывает чистый и грязный PnL. Имеет безопасный fallback на случай отсутствия сделок.
- **Синхронизация (`execution_live.py`)**:
  - Метод `sync_state` при обнаружении закрытия позиции вызывает `get_real_trade_details` для обновления параметров сделки (размер, цены, комиссии, PnL) фактическими данными с биржи перед записью в Redis.
- **Интерфейс Telegram (`telegram_bot.py`)**:
  - При ручном закрытии позиции добавлено ожидание в 1 секунду для регистрации сделки на бирже, после чего вызывается `get_real_trade_details` для отправки в Telegram уведомления с точными ценами и реальными комиссиями.

### Шаг 72: Реализация Агента оптимизации параметров сделок (Trade Optimizer Agent)
- **Создание `optimizer_agent.py`**:
  - Создан класс `TradeOptimizerAgent`, анализирующий историю сделок из Redis и вычисляющий статистические показатели (Win Rate, Profit, количество сделок) по срезам: глобально, по монетам и по конкретным сетапам `(coin, tf, side)`.
  - Метод `predict_probability()` оценивает вероятность успеха нового сетапа на основе его истории и динамически выбирает стратегию трейлинг-стопа (`TRENDING`, `SCALPING`, `FIXED`), а также корректирует риск-множитель для сделки (повышенный 1.3x для уверенных сетапов, 1.0x для стандартных, 0.5x для слабых).
- **Обновление моделей (`strategy.py`, `execution.py`)**:
  - В классы `SignalCandidate`, `PendingOrder` и `Position` внедрены поля `setup_probability`, `trailing_strategy` и `suggested_risk_pct`.
  - Все методы сериализации (`to_dict`/`from_dict`) обновлены для корректной передачи и сохранения этих параметров в Redis.
- **Интеграция в движки исполнения (`execution_live.py`, `execution.py`)**:
  - Метод `maybe_place_order()` теперь обращается к оптимизатору для получения вероятности сетапа, корректирует расчетный объем `qty` ордера на основе dynamic risk percentage и сохраняет эти метаданные в Redis.
  - Метод `_apply_trailing()` использует стратегию трейлинг-стопа: `SCALPING` переносит SL в безубыток при 40% движения и агрессивно подтягивает стопы; `TRENDING` дает цене «дышать» (безубыток при 70%); `FIXED` отключает трейлинг.
- **Управление и статус в Telegram (`telegram_bot.py`)**:
  - Статус активных позиций и отложенных ордеров в Telegram теперь отображает их вероятность и стратегию трейлинга.
  - Добавлены команды `/optimizer` и `/opt`, выводящие подробный аналитический отчет агента по сделкам и его текущие рекомендации для каждой торговой пары.
- **Запуск (`main.py`)**:
  - Оптимизатор интегрирован в цикл `market_loop` и передается в качестве аргумента при инициализации `ExecutionLiveRun`.

### Шаг 73: Устранение нулевого PnL при закрытии и уточнение уведомлений
- **API `userFills` (`hyperliquid_api.py`)**:
  - Метод `get_real_trade_details()` усилен ретраями: при отсутствии закрывающих `fills` бот повторно запрашивает историю несколько раз с задержкой, чтобы дождаться подтверждения сделки на бирже.
  - Если биржа все еще не вернула `fills`, вместо ошибочного `0.0` используется расчетный fallback `Net PnL = Gross PnL - Fee(open+close)` на основе фактических `entry/exit/qty`, что устраняет проблему отображения `00000`.
  - В ответ метода добавлены поля `source` и `success`, чтобы различать подтвержденный `PnL` с биржи и временную оценку.
- **Синхронизация закрытия (`execution_live.py`)**:
  - При закрытии позиции в Redis теперь сохраняются `pnl_source` и `pnl_confirmed`, а также передаются в событие `position_closed` для дальнейшего отображения в Telegram.
- **Ручное закрытие (`telegram_bot.py`)**:
  - Уведомление о ручном закрытии приведено к формату: дата, способ закрытия, цена входа/выхода, количество, `Net PnL`, комиссия и доступный баланс.
  - Если биржевые `fills` еще не подтверждены, в сообщении явно показывается, что `PnL` временно рассчитан по fallback.
- **Авто-уведомления (`main.py`)**:
  - Сообщение `position_closed` теперь отдельно показывает дату и способ закрытия, цену входа/выхода и количество.
  - Добавлено явное предупреждение, если `PnL` еще не подтвержден биржевыми `fills`.

### Шаг 74: Переписывание логики сетапа на два последних кластера и ATR-фильтр
- **Волатильность (`indicators.py`)**:
  - Добавлен расчет `ATR`, который используется как базовая мера волатильности для оценки допустимой ширины сетапа между последними long/short кластерами.
- **Стратегия (`strategy.py`)**:
  - `StrategyEngine` больше не ищет разрозненные фракталы по старой эвристике `close > teeth / close < teeth`.
  - Сетап строится строго от двух последних подтвержденных противоположных кластеров: последнего `HIGH` и последнего `LOW`.
  - Добавлена проверка, что до этих кластеров уже произошло хотя бы одно пересечение каждой пары линий аллигатора (`jaw/teeth`, `teeth/lips`, `jaw/lips`), то есть “сон аллигатора” зафиксирован именно до текущего сетапа.
  - Добавлен фильтр ширины сетапа: расстояние между двумя последними кластерами переводится в `ATR`, и бот отбрасывает слишком широкий сетап (`cluster_spread_too_wide`) при превышении допустимого лимита.
  - Входы и стопы смещены на тик от уровней кластеров:
    - `LONG`: `entry = high_cluster + tick`, `stop = low_cluster - tick`
    - `SHORT`: `entry = low_cluster - tick`, `stop = high_cluster + tick`
  - В `ctx` сигнала сохраняются новые диагностические поля: `atr`, `cluster_spread`, `cluster_spread_atr`, `pre_setup_crosses`.

### Шаг 75: Реализация нового циклического трейлинга с виртуальным TP
- **Модель позиции (`execution.py`)**:
  - В `Position` добавлен флаг `take_profit_active`, чтобы отделять реальный `TP` на бирже от расчетного внутреннего `TP`, который используется только для следующего шага трейлинга.
- **Новый алгоритм трейлинга (`execution.py`, `execution_live.py`)**:
  - Реализована логика:
    - при достижении `70%` текущего `TP` стоп переносится в `BE + 10%` дистанции,
    - при достижении `95%` стоп переносится на `90%` дистанции, а внутренний `TP` расширяется на `+50%`,
    - далее циклически при достижении `90%` текущего внутреннего `TP` стоп переносится на `80%`, а внутренний `TP` увеличивается на `+35%`.
  - Логика реализована зеркально для `LONG` и `SHORT`.
- **Реальная торговля (`execution_live.py`)**:
  - После перехода на виртуальный `TP` бот на бирже больше не перевыставляет `TP`, а работает только со `SL`, как и требовалось.
  - В событие `position_updated` добавлены `uPnL` и флаг `take_profit_active`.
- **Интерфейс (`main.py`, `telegram_bot.py`)**:
  - Уведомление о трейлинге теперь показывает `Entry`, новый `SL`, новый `TP` с явной пометкой `TP(virt)`, количество и текущий `uPnL`.
  - В экране позиций `TP` также помечается как виртуальный, если он больше не выставлен на бирже.

### Шаг 76: Фундамент для аналитического агента по сделкам
- **Признаки сетапа (`strategy.py`)**:
  - В `SignalCandidate` добавлено поле `setup_features`, в которое сохраняются признаки сетапа: `ATR`, ширина сетапа, ширина в `ATR`, бакет ширины (`tight/normal/wide`), метрики “сна аллигатора” и временные метки последних кластеров.
- **Передача через весь жизненный цикл сделки (`execution.py`, `execution_live.py`, `main.py`)**:
  - `setup_features` теперь проходят через `SignalCandidate -> PendingOrder -> Position -> trade`.
  - В закрытую сделку дополнительно пишутся `setup_probability`, `trailing_strategy`, `setup_features` и количество шагов трейлинга.
- **Агент (`optimizer_agent.py`)**:
  - История сделок теперь анализируется не только по `pair/tf/side`, но и по типу трейлинга и `ATR`-бакетам ширины сетапа.
  - `predict_probability()` начинает учитывать `cluster_spread_atr_bucket` текущего сетапа: для широких сетапов слегка снижает вероятность и предпочитает более защитный сценарий.
- **Telegram (`telegram_bot.py`)**:
  - Команда `/optimizer` дополнительно показывает статистику по `ATR`-бакетам сетапов, что дает базу для дальнейшего развития полноценного decision-agent.


